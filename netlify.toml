[build]
  command = "cd frontend && npm install && npm run build"
  publish = "frontend/build"

[build.environment]
  NODE_VERSION = "18"
  REACT_APP_API_URL = "https://expandir-terapiaintegrativa-production.up.railway.app"
